ComfyUI-H3-Continuum 3.2.1 - Package Validation
Date: 2026-08-13

Release hardening scope:
- Graph + runtime MODEL/CLIP/VAE sampling contract
- Chunk file SHA-256 verification
- Run Storage automatic ID fallback and Storage-only partial regeneration
- V3.2.1 Windows installer/runtime verifier
- Explicit CI numpy/safetensors test dependencies

Required automated validation:
- python -m compileall -q .
- python -m pytest -q -p no:cacheprovider

Runtime validation still required before a public Release:
- Save, interrupt and resume a multi-chunk run
- FL2VA -> Ref2VA -> FL2VA Revision restoration
- MODEL/LoRA change creates a separate Revision
- Corrupted stored Chunk is rejected
- Spectrum Actual Prefix 2 is claimed only when the receiver accepts it
